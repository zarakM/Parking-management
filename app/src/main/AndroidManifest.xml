<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pk.zarsh.parkingmanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Signin"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainAdminActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".MainUserActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".ParkingAreas" />
        <activity android:name=".UserList" />
        <activity android:name=".Signup" />
        <activity android:name=".Reply" />
        <activity android:name=".Slots" />
        <activity android:name=".BookingDetail" />
        <activity android:name=".Feedback" />
        <activity android:name=".BookingDefense" />
        <activity android:name=".BookingTariq" />
        <activity android:name=".BookingGulshan" />
        <activity android:name=".SpecificBooking" />
        <activity android:name=".Receipt"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>